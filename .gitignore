target/
*.profraw
*.profdata
lcov.info

.env
!.env.example

# Local cargo overrides (e.g. a shared target dir). Never committed: an
# accidental target-dir override silently opts a build out of shared artifact
# caching.
.cargo/config.toml

# Worktrees created by the `worktree` helper.
worktrees/
.claude/worktrees/

# Editor and OS noise
.idea/
.vscode/
*.swp
.DS_Store

# GitHub App credentials from scripts/create-github-app.sh. Contains a private
# key: move it to a secret store and delete the local copy.
.tinysweeper-app.json
