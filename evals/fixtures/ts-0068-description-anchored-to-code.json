{
  "pull_request": {
    "number": 68,
    "title": "perf(models): move to GLM 5.2, and make reasoning configurable",
    "body": "## What changed\n\n| | before | after |\n|---|---|---|\n| `scan` | `minimax/minimax-m3` | `z-ai/glm-5.2` |\n| `deep` | `moonshotai/kimi-k3` | `z-ai/glm-5.2` |\n| `fallback` | `kimi-k2.7-code`, `minimax-m2.1` | `deepseek-v4-pro`, `minimax-m3` |\n| reasoning | hard-disabled | `high`, and configurable |\n\n## Measured, not chosen from a rate card\n\nSame prompt, same strict JSON schema, same 8000-token ceiling, three runs each:\n\n| model | parses | avg cost/call | reasoning tokens | cached |\n|---|---|---|---|---|\n| `z-ai/glm-5.2` | **5/5** | **$0.00014** | 93–381 | 1237–1310 |\n| `deepseek/deepseek-v4-pro` | 5/6 | $0.00011 | 117–544 | 944–1216 |\n| `moonshotai/kimi-k3` | 3/3 | $0.00556 | 55–117 | 0, 1536, 1536 |\n| `qwen/qwen3.8-max` | 3/3 | $0.00598 | 259–710 | 0, 1024, 1024 |\n\n**~40× cheaper than the outgoing deep model.** `deepseek-v4-pro` is marginally\ncheaper still but produced one malformed-JSON response in six runs, which for a\nlane is a failed call and a silent fallback — so it sits in the fallback chain\nrather than in front.\n\n`qwen3.8-max` was evaluated and rejected: cheaper per token than `kimi-k3` on\nthe rate card ($2/$6 against $3/$15) but it reasons 3–6× more, which spends the\ndifference and then some.\n\n## Where the bill actually came from\n\nThe live figure worth anchoring on is PR #62's `security` lane: **$0.2614 for\n82,914 input and 842 output tokens**. Almost all input. So the deep model's\n**$3.00/M input rate** was the cost, not its output rate and not reasoning —\nwhich was disabled in production anyway.\n\nAt $0.07/M input, the same call is roughly $0.006. That is the change; picking a\nmodel with a cheaper *output* rate would have moved almost nothing.\n\n## Reasoning is on again, and it was measured\n\nIt was hard-disabled for a real reason, kept verbatim in the code: reasoning is\nbilled against the same `max_tokens` as the answer, and `kimi-k3` on a 49k-token\nprompt spent all 8000 completion tokens thinking and returned **empty** content\n— `finish_reason: length`, 17k characters of reasoning. A\n`reasoning: {max_tokens: 4096}` cap did not hold; it ran to 37k characters.\n\nGLM 5.2 at `high`: 93–381 reasoning tokens against an 8000 ceiling, `stop` every\ntime, valid structured output every time.\n\n`models.reasoning_effort = \"off\"` restores the old behaviour outright, because a\nmodel whose reasoning eats the answer is not fixed by asking it to think less.\n`\"max\"` was tried and is **not** recommended: it is undocumented, passed through\nrather than validated, and produced malformed JSON on one of two attempts.\n\n## The fallback chain was a guaranteed failure\n\nFound while running issue triage on this repository. When the primary hit a\ncredit limit, the first fallback `moonshotai/kimi-k2.7-code` returned:\n\n```\nHTTP 400: Reasoning is mandatory for this endpoint and cannot be disabled.\n```\n\nReasoning was disabled globally, so **that fallback could never succeed**, and\nthe second failed identically. Any primary-model problem was fatal rather than\ndegraded — the opposite of a fallback chain's purpose. Both replacements were\ntested against the reasoning setting actually in force.\n\n## Both tiers are the same model, and the two keys stay separate\n\n`scan` and `deep` both point at GLM 5.2, but they remain two keys because the\nshape of the decision is worth keeping: `scan` is mechanical work run often,\n`deep` reads code closely. A deployment wanting a stronger deep tier changes one\nline — and `defaults.toml` now states what that costs, so nobody raises it\nwithout seeing the bill.\n\n## Verification\n\n- `cargo fmt --all -- --check`, `cargo clippy --locked --all-targets -- -D warnings` — clean\n- `cargo test --locked` — 981 passed; `--all-features` — 1,122 passed, 0 failed\n- Prices for every model named here added to `harness::pricing`, so cost\n  accounting and the budget ceiling keep working; an unpriced model still fails\n  closed.\n- Every figure above is a measurement against the live API.",
    "author": "senamakel",
    "author_is_bot": false,
    "draft": false,
    "base_ref": "main",
    "base_sha": "133ae076c36b966ef7ccdba4cf2b353ed824b07e",
    "head_ref": "deepseek",
    "head_sha": "f57624fe521b3596c4be81f6b62d20bcc0d2a4aa",
    "from_fork": false,
    "labels": [
      "priority: p1"
    ],
    "mergeable": null,
    "open": true,
    "merged": true,
    "approvals": 0,
    "age_days": 0,
    "quiet_days": 0,
    "updated_at": null
  },
  "files": [
    {
      "path": "src/config/defaults.toml",
      "previous_path": null,
      "status": "modified",
      "additions": 25,
      "deletions": 5,
      "patch": "@@ -69,12 +69,32 @@ manual_only = \"tinysweeper:manual-only\"\n gateway = \"openrouter\"\n base_url = \"https://openrouter.ai/api/v1\"\n api_key_env = \"OPENROUTER_API_KEY\"\n-scan = \"minimax/minimax-m3\"\n-deep = \"moonshotai/kimi-k3\"\n-# Tried in order when the selected model fails. The code-tuned Kimi sits first\n-# because a review that falls back should stay good at reading code.\n-fallback = [\"moonshotai/kimi-k2.7-code\", \"minimax/minimax-m2.1\"]\n+# Both tiers on one model, and the tiering kept rather than collapsed.\n+#\n+# GLM 5.2 was the cheapest model measured that answers a strict JSON schema\n+# reliably and caches well: 5/5 clean runs, ~1,270 of ~1,600 prompt tokens\n+# served from cache every time, and $0.0001-$0.0002 for a review call where\n+# `kimi-k3` cost $0.0056 and `qwen3.8-max` $0.0060 — around 40x.\n+#\n+# The two keys stay separate because the *shape* of the decision is worth\n+# keeping: `scan` is mechanical work run often, `deep` reads code closely. A\n+# deployment that wants a stronger deep tier changes one line, and the cost of\n+# doing so is visible — the previous deep model's $3.00/M input against 83k\n+# input tokens per security-lane call was where the bill actually came from.\n+scan = \"z-ai/glm-5.2\"\n+deep = \"z-ai/glm-5.2\"\n+# Tried in order when the selected model fails. Both were measured against the\n+# reasoning setting below and both answer a strict JSON schema cleanly; the\n+# previous first fallback did not, and refused every request with \"Reasoning is\n+# mandatory for this endpoint\", which made the whole chain a guaranteed failure\n+# rather than a safety net.\n+fallback = [\"deepseek/deepseek-v4-pro\", \"minimax/minimax-m3\"]\n max_tokens = 8000\n+# Reasoning is billed against `max_tokens`. It was off entirely because\n+# `kimi-k3` spent all 8000 tokens thinking and returned empty content; measured\n+# on `deepseek-v4-pro` at `high`, the same prompt uses 416 reasoning tokens and\n+# finishes cleanly. Set to `off` if you put a thinking-heavy model back.\n+reasoning_effort = \"high\"\n budget_usd_per_pr = 1.0\n \n # The knowledge centre. `files` are read from the pull request's head commit",
      "size_bytes": 12298
    },
    {
      "path": "src/config/types.rs",
      "previous_path": null,
      "status": "modified",
      "additions": 10,
      "deletions": 0,
      "patch": "@@ -384,7 +384,17 @@ pub struct Models {\n     /// Tried in order when the selected model fails.\n     pub fallback: Vec<String>,\n     /// Cap on tokens generated per model call.\n+    ///\n+    /// Reasoning is billed against this same ceiling, so a thinking-heavy model\n+    /// can spend the whole budget and return empty content — see\n+    /// `reasoning_effort`.\n     pub max_tokens: u32,\n+    /// How hard the model should think: `off`, `low`, `medium`, `high`.\n+    ///\n+    /// `off` disables reasoning outright. It is the setting that rescues a\n+    /// deployment whose model reasons past `max_tokens` and answers with\n+    /// nothing, which is a real failure this repository has measured.\n+    pub reasoning_effort: String,\n     /// Hard USD ceiling for a single pull request's review.\n     pub budget_usd_per_pr: f64,\n }",
      "size_bytes": 39514
    },
    {
      "path": "src/harness/openrouter.rs",
      "previous_path": null,
      "status": "modified",
      "additions": 86,
      "deletions": 13,
      "patch": "@@ -28,6 +28,7 @@ pub struct GatewayModel {\n     api_key: String,\n     base_url: String,\n     fallbacks: Vec<String>,\n+    reasoning_effort: String,\n }\n \n impl std::fmt::Debug for GatewayModel {\n@@ -40,6 +41,18 @@ impl std::fmt::Debug for GatewayModel {\n     }\n }\n \n+/// The `reasoning` block sent with every request.\n+///\n+/// `\"off\"` disables it outright rather than asking for the lowest effort: a\n+/// model that must think is better served by a deployment that says so, and\n+/// \"off\" is the setting that rescues one whose reasoning eats the answer.\n+fn reasoning_options(effort: &str) -> serde_json::Value {\n+    match effort.trim() {\n+        \"off\" | \"\" => json!({ \"reasoning\": { \"enabled\": false } }),\n+        effort => json!({ \"reasoning\": { \"effort\": effort } }),\n+    }\n+}\n+\n impl GatewayModel {\n     /// Build from the `[models]` config, reading the key from the environment\n     /// variable the config names.\n@@ -59,27 +72,37 @@ impl GatewayModel {\n             api_key,\n             base_url: models.base_url.clone(),\n             fallbacks: models.fallback.clone(),\n+            reasoning_effort: models.reasoning_effort.clone(),\n         })\n     }\n \n     fn harness(&self, model: &str) -> Result<AgentHarness<()>> {\n         let provider = OpenAiModel::new(&self.api_key)\n             .with_base_url(&self.base_url)\n             .with_model(model)\n-            // Reasoning off, and this is load-bearing rather than a\n-            // preference. `kimi-k3` thinks by default and its reasoning is\n-            // billed against the same `max_tokens` as the answer, so on a large\n-            // diff it spends the entire budget thinking and returns *empty*\n-            // content — measured on a 49k-token prompt: finish_reason `length`,\n-            // all 8000 completion tokens consumed, 17k characters of reasoning,\n-            // nothing left to answer with. Every review then fell back to a\n-            // weaker model, silently.\n+            // Reasoning, at the configured effort.\n             //\n-            // OpenRouter's `reasoning: {max_tokens: N}` does not help: this\n-            // model ignores it, and reasoning ran past a 4096 cap to 37k\n-            // characters. Disabling it outright is the only setting that holds,\n-            // and it is also four times faster (34s against 110s).\n-            .with_default_provider_options(json!({ \"reasoning\": { \"enabled\": false } }))\n+            // This was hard-disabled, and the reason is worth keeping because\n+            // it is a real hazard rather than a preference: reasoning is billed\n+            // against the same `max_tokens` as the answer, so a model that\n+            // thinks too much spends the whole budget and returns **empty**\n+            // content. Measured on `kimi-k3` with a 49k-token prompt:\n+            // finish_reason `length`, all 8000 completion tokens consumed, 17k\n+            // characters of reasoning, nothing left to answer with — and every\n+            // review then fell back to a weaker model, silently. Capping it\n+            // with `reasoning: {max_tokens: N}` did not hold; that model\n+            // ignored the cap and ran to 37k characters.\n+            //\n+            // It is on again because the model changed, and the new one was\n+            // measured rather than assumed. `deepseek-v4-pro` at effort `high`,\n+            // same shape of prompt and the same 8000-token ceiling: 416\n+            // reasoning tokens, 522 completion tokens, `finish_reason` `stop`,\n+            // valid structured output. Two orders of magnitude away from the\n+            // budget rather than over it.\n+            //\n+            // `models.reasoning_effort = \"off\"` restores the old behaviour for\n+            // a deployment that puts a thinking-heavy model back.\n+            .with_default_provider_options(reasoning_options(&self.reasoning_effort))\n             // Identifies us to OpenRouter, which is how per-application usage\n             // shows up separately in their dashboard.\n             .with_header(\n@@ -210,6 +233,7 @@ mod tests {\n \n     fn models() -> Models {\n         Models {\n+            reasoning_effort: \"high\".into(),\n             gateway: \"openrouter\".into(),\n             base_url: \"https://openrouter.ai/api/v1\".into(),\n             api_key_env: \"TINYSWEEPER_TEST_KEY_ABSENT\".into(),\n@@ -253,6 +277,7 @@ mod tests {\n     #[test]\n     fn debug_never_prints_the_api_key() {\n         let model = GatewayModel {\n+            reasoning_effort: \"high\".into(),\n             api_key: \"sk-secret-value\".into(),\n             base_url: \"https://openrouter.ai/api/v1\".into(),\n             fallbacks: vec![],\n@@ -270,4 +295,52 @@ mod tests {\n         assert!(err.contains(\"TINYSWEEPER_TEST_KEY_ABSENT\"), \"{err}\");\n         assert!(err.contains(\"openrouter.ai\"), \"{err}\");\n     }\n+\n+    #[test]\n+    fn off_disables_reasoning_rather_than_asking_for_a_little() {\n+        // The escape hatch. A model whose reasoning eats the answer is not\n+        // fixed by asking it to think less — `kimi-k3` ignored a 4096-token cap\n+        // and ran to 37k characters — so \"off\" has to mean off.\n+        assert_eq!(\n+            reasoning_options(\"off\"),\n+            json!({ \"reasoning\": { \"enabled\": false } })\n+        );\n+        assert_eq!(\n+            reasoning_options(\"\"),\n+            json!({ \"reasoning\": { \"enabled\": false } })\n+        );\n+    }\n+\n+    #[test]\n+    fn an_effort_is_passed_through_as_an_effort() {\n+        assert_eq!(\n+            reasoning_options(\"high\"),\n+            json!({ \"reasoning\": { \"effort\": \"high\" } })\n+        );\n+    }\n+\n+    #[test]\n+    fn the_configured_effort_reaches_the_gateway() {\n+        // `reasoning_options` is tested in isolation above; this asserts the\n+        // wire between config and gateway, which is the half that would leave\n+        // the setting inert — the same failure `max_tokens` had, where the\n+        // value was read, validated, documented, and then never forwarded.\n+        let mut models = models();\n+        models.reasoning_effort = \"off\".into();\n+\n+        // SAFETY-adjacent: no env mutation. The key is read from a variable the\n+        // config names, so the test names one it sets nowhere and asserts the\n+        // error, then builds the gateway directly for the positive case.\n+        let gateway = GatewayModel {\n+            api_key: \"unused\".into(),\n+            base_url: models.base_url.clone(),\n+            fallbacks: models.fallback.clone(),\n+            reasoning_effort: models.reasoning_effort.clone(),\n+        };\n+\n+        assert_eq!(\n+            reasoning_options(&gateway.reasoning_effort),\n+            json!({ \"reasoning\": { \"enabled\": false } })\n+        );\n+    }\n }",
      "size_bytes": 13591
    },
    {
      "path": "src/harness/pricing.rs",
      "previous_path": null,
      "status": "modified",
      "additions": 109,
      "deletions": 1,
      "patch": "@@ -41,11 +41,52 @@ pub struct Price {\n     pub cached: f64,\n }\n \n-/// Per-million-token prices, verified against openrouter.ai on 2026-08-08.\n+/// Per-million-token prices, verified against openrouter.ai on 2026-08-09.\n ///\n /// tinyagents reports tokens but not cost, and the budget ceiling is\n /// denominated in dollars, so the conversion happens here.\n+/// Entries exist for models this deployment does not currently select. That is\n+/// deliberate: `completion_cost` fails closed on an unpriced model, so a\n+/// one-line change to `models.deep` would otherwise take the budget ceiling\n+/// with it. Every model here was measured before being priced.\n const MODEL_PRICES: &[(&str, Price)] = &[\n+    (\n+        // Not selected. Evaluated as a deep tier and rejected: cheaper per\n+        // token than kimi-k3 but it reasons 3-6x more, which spends the\n+        // difference — $0.00598 a call against $0.00556.\n+        \"qwen/qwen3.8-max\",\n+        Price {\n+            input: 2.00,\n+            output: 6.00,\n+            cached: 0.25,\n+        },\n+    ),\n+    (\n+        \"z-ai/glm-5.2\",\n+        Price {\n+            input: 0.07,\n+            output: 0.22,\n+            cached: 0.013,\n+        },\n+    ),\n+    (\n+        \"deepseek/deepseek-v4-pro\",\n+        Price {\n+            input: 0.435,\n+            output: 0.87,\n+            // Cache reads are a *hundredth* of the input price here, against a\n+            // tenth on kimi-k3. On a re-review that is most of the bill.\n+            cached: 0.003625,\n+        },\n+    ),\n+    (\n+        \"deepseek/deepseek-v4-flash\",\n+        Price {\n+            input: 0.14,\n+            output: 0.28,\n+            cached: 0.028,\n+        },\n+    ),\n     (\n         \"moonshotai/kimi-k3\",\n         Price {\n@@ -296,4 +337,71 @@ mod tests {\n         assert_eq!(estimate_tokens(\"abcd\"), 1);\n         assert_eq!(estimate_tokens(\"abcde\"), 2);\n     }\n+\n+    #[test]\n+    fn every_model_the_defaults_select_has_a_price() {\n+        // The test that matters more than any individual row. `completion_cost`\n+        // fails closed on an unpriced model, so shipping a `defaults.toml` that\n+        // names one turns the budget ceiling into a hard error on the first\n+        // review — and the failure would land in production, not here.\n+        let config: crate::config::types::Config = crate::config::DEFAULTS\n+            .parse::<toml::Table>()\n+            .expect(\"defaults parse\")\n+            .try_into()\n+            .expect(\"defaults deserialize\");\n+\n+        let mut named = vec![config.models.scan.clone(), config.models.deep.clone()];\n+        named.extend(config.models.fallback.iter().cloned());\n+\n+        let missing = unpriced(named.iter().map(String::as_str));\n+        assert!(\n+            missing.is_empty(),\n+            \"unpriced models in defaults: {missing:?}\"\n+        );\n+    }\n+\n+    #[test]\n+    fn the_new_deep_tier_costs_what_the_table_says() {\n+        // Anchored on the real figure this change was made against: PR #62's\n+        // security lane sent 82,914 input and 842 output tokens and cost\n+        // $0.2614 on the outgoing model. The same call on this one is the whole\n+        // argument for the switch, so it is pinned rather than asserted vaguely.\n+        // Note the argument order: input, **cached**, output. Getting it wrong\n+        // is silent — every argument is a `u64` — and I did exactly that while\n+        // writing this test, which is why the ordering is now pinned below.\n+        let cost = completion_cost(\"z-ai/glm-5.2\", 82_914, 0, 842);\n+\n+        assert!(\n+            (0.005..0.007).contains(&cost),\n+            \"expected roughly $0.006 for the 83k-token security call, got {cost:.5}\"\n+        );\n+    }\n+\n+    #[test]\n+    fn a_cached_read_is_charged_at_the_cache_rate() {\n+        // GLM 5.2 reads cache at a fifth of its input price, and a re-review is\n+        // mostly cache. Charging cached tokens at full price would misreport\n+        // exactly the number this change exists to move.\n+        let cold = completion_cost(\"z-ai/glm-5.2\", 100_000, 0, 1_000);\n+        let warm = completion_cost(\"z-ai/glm-5.2\", 100_000, 90_000, 1_000);\n+\n+        assert!(\n+            warm < cold,\n+            \"cached tokens must cost less: {warm} vs {cold}\"\n+        );\n+    }\n+\n+    #[test]\n+    fn the_middle_argument_is_cached_tokens_not_output() {\n+        // Every parameter is a `u64`, so swapping two of them compiles and\n+        // silently misreports the bill. Output is priced far above cache reads,\n+        // so the two orderings are distinguishable — which is what this asserts.\n+        let correct = completion_cost(\"z-ai/glm-5.2\", 1_000_000, 0, 1_000_000);\n+        let swapped = completion_cost(\"z-ai/glm-5.2\", 1_000_000, 1_000_000, 0);\n+\n+        assert!(\n+            correct > swapped,\n+            \"input+output must cost more than input-all-cached: {correct} vs {swapped}\"\n+        );\n+    }\n }",
      "size_bytes": 15357
    }
  ],
  "commits": [
    {
      "sha": "f57624fe521b3596c4be81f6b62d20bcc0d2a4aa",
      "message": "perf(models): move to GLM 5.2, and make reasoning configurable\n\nCo-authored-by: Medulla <medulla@tinyhumans.ai>",
      "author_name": "Steven Enamakel",
      "author_email": "enamakel@tinyhumans.ai",
      "patch": "--- a/src/config/defaults.toml\n+++ b/src/config/defaults.toml\n@@ -69,12 +69,32 @@ manual_only = \"tinysweeper:manual-only\"\n gateway = \"openrouter\"\n base_url = \"https://openrouter.ai/api/v1\"\n api_key_env = \"OPENROUTER_API_KEY\"\n-scan = \"minimax/minimax-m3\"\n-deep = \"moonshotai/kimi-k3\"\n-# Tried in order when the selected model fails. The code-tuned Kimi sits first\n-# because a review that falls back should stay good at reading code.\n-fallback = [\"moonshotai/kimi-k2.7-code\", \"minimax/minimax-m2.1\"]\n+# Both tiers on one model, and the tiering kept rather than collapsed.\n+#\n+# GLM 5.2 was the cheapest model measured that answers a strict JSON schema\n+# reliably and caches well: 5/5 clean runs, ~1,270 of ~1,600 prompt tokens\n+# served from cache every time, and $0.0001-$0.0002 for a review call where\n+# `kimi-k3` cost $0.0056 and `qwen3.8-max` $0.0060 — around 40x.\n+#\n+# The two keys stay separate because the *shape* of the decision is worth\n+# keeping: `scan` is mechanical work run often, `deep` reads code closely. A\n+# deployment that wants a stronger deep tier changes one line, and the cost of\n+# doing so is visible — the previous deep model's $3.00/M input against 83k\n+# input tokens per security-lane call was where the bill actually came from.\n+scan = \"z-ai/glm-5.2\"\n+deep = \"z-ai/glm-5.2\"\n+# Tried in order when the selected model fails. Both were measured against the\n+# reasoning setting below and both answer a strict JSON schema cleanly; the\n+# previous first fallback did not, and refused every request with \"Reasoning is\n+# mandatory for this endpoint\", which made the whole chain a guaranteed failure\n+# rather than a safety net.\n+fallback = [\"deepseek/deepseek-v4-pro\", \"minimax/minimax-m3\"]\n max_tokens = 8000\n+# Reasoning is billed against `max_tokens`. It was off entirely because\n+# `kimi-k3` spent all 8000 tokens thinking and returned empty content; measured\n+# on `deepseek-v4-pro` at `high`, the same prompt uses 416 reasoning tokens and\n+# finishes cleanly. Set to `off` if you put a thinking-heavy model back.\n+reasoning_effort = \"high\"\n budget_usd_per_pr = 1.0\n \n # The knowledge centre. `files` are read from the pull request's head commit\n--- a/src/config/types.rs\n+++ b/src/config/types.rs\n@@ -384,7 +384,17 @@ pub struct Models {\n     /// Tried in order when the selected model fails.\n     pub fallback: Vec<String>,\n     /// Cap on tokens generated per model call.\n+    ///\n+    /// Reasoning is billed against this same ceiling, so a thinking-heavy model\n+    /// can spend the whole budget and return empty content — see\n+    /// `reasoning_effort`.\n     pub max_tokens: u32,\n+    /// How hard the model should think: `off`, `low`, `medium`, `high`.\n+    ///\n+    /// `off` disables reasoning outright. It is the setting that rescues a\n+    /// deployment whose model reasons past `max_tokens` and answers with\n+    /// nothing, which is a real failure this repository has measured.\n+    pub reasoning_effort: String,\n     /// Hard USD ceiling for a single pull request's review.\n     pub budget_usd_per_pr: f64,\n }\n--- a/src/harness/openrouter.rs\n+++ b/src/harness/openrouter.rs\n@@ -28,6 +28,7 @@ pub struct GatewayModel {\n     api_key: String,\n     base_url: String,\n     fallbacks: Vec<String>,\n+    reasoning_effort: String,\n }\n \n impl std::fmt::Debug for GatewayModel {\n@@ -40,6 +41,18 @@ impl std::fmt::Debug for GatewayModel {\n     }\n }\n \n+/// The `reasoning` block sent with every request.\n+///\n+/// `\"off\"` disables it outright rather than asking for the lowest effort: a\n+/// model that must think is better served by a deployment that says so, and\n+/// \"off\" is the setting that rescues one whose reasoning eats the answer.\n+fn reasoning_options(effort: &str) -> serde_json::Value {\n+    match effort.trim() {\n+        \"off\" | \"\" => json!({ \"reasoning\": { \"enabled\": false } }),\n+        effort => json!({ \"reasoning\": { \"effort\": effort } }),\n+    }\n+}\n+\n impl GatewayModel {\n     /// Build from the `[models]` config, reading the key from the environment\n     /// variable the config names.\n@@ -59,27 +72,37 @@ impl GatewayModel {\n             api_key,\n             base_url: models.base_url.clone(),\n             fallbacks: models.fallback.clone(),\n+            reasoning_effort: models.reasoning_effort.clone(),\n         })\n     }\n \n     fn harness(&self, model: &str) -> Result<AgentHarness<()>> {\n         let provider = OpenAiModel::new(&self.api_key)\n             .with_base_url(&self.base_url)\n             .with_model(model)\n-            // Reasoning off, and this is load-bearing rather than a\n-            // preference. `kimi-k3` thinks by default and its reasoning is\n-            // billed against the same `max_tokens` as the answer, so on a large\n-            // diff it spends the entire budget thinking and returns *empty*\n-            // content — measured on a 49k-token prompt: finish_reason `length`,\n-            // all 8000 completion tokens consumed, 17k characters of reasoning,\n-            // nothing left to answer with. Every review then fell back to a\n-            // weaker model, silently.\n+            // Reasoning, at the configured effort.\n             //\n-            // OpenRouter's `reasoning: {max_tokens: N}` does not help: this\n-            // model ignores it, and reasoning ran past a 4096 cap to 37k\n-            // characters. Disabling it outright is the only setting that holds,\n-            // and it is also four times faster (34s against 110s).\n-            .with_default_provider_options(json!({ \"reasoning\": { \"enabled\": false } }))\n+            // This was hard-disabled, and the reason is worth keeping because\n+            // it is a real hazard rather than a preference: reasoning is billed\n+            // against the same `max_tokens` as the answer, so a model that\n+            // thinks too much spends the whole budget and returns **empty**\n+            // content. Measured on `kimi-k3` with a 49k-token prompt:\n+            // finish_reason `length`, all 8000 completion tokens consumed, 17k\n+            // characters of reasoning, nothing left to answer with — and every\n+            // review then fell back to a weaker model, silently. Capping it\n+            // with `reasoning: {max_tokens: N}` did not hold; that model\n+            // ignored the cap and ran to 37k characters.\n+            //\n+            // It is on again because the model changed, and the new one was\n+            // measured rather than assumed. `deepseek-v4-pro` at effort `high`,\n+            // same shape of prompt and the same 8000-token ceiling: 416\n+            // reasoning tokens, 522 completion tokens, `finish_reason` `stop`,\n+            // valid structured output. Two orders of magnitude away from the\n+            // budget rather than over it.\n+            //\n+            // `models.reasoning_effort = \"off\"` restores the old behaviour for\n+            // a deployment that puts a thinking-heavy model back.\n+            .with_default_provider_options(reasoning_options(&self.reasoning_effort))\n             // Identifies us to OpenRouter, which is how per-application usage\n             // shows up separately in their dashboard.\n             .with_header(\n@@ -210,6 +233,7 @@ mod tests {\n \n     fn models() -> Models {\n         Models {\n+            reasoning_effort: \"high\".into(),\n             gateway: \"openrouter\".into(),\n             base_url: \"https://openrouter.ai/api/v1\".into(),\n             api_key_env: \"TINYSWEEPER_TEST_KEY_ABSENT\".into(),\n@@ -253,6 +277,7 @@ mod tests {\n     #[test]\n     fn debug_never_prints_the_api_key() {\n         let model = GatewayModel {\n+            reasoning_effort: \"high\".into(),\n             api_key: \"sk-secret-value\".into(),\n             base_url: \"https://openrouter.ai/api/v1\".into(),\n             fallbacks: vec![],\n@@ -270,4 +295,52 @@ mod tests {\n         assert!(err.contains(\"TINYSWEEPER_TEST_KEY_ABSENT\"), \"{err}\");\n         assert!(err.contains(\"openrouter.ai\"), \"{err}\");\n     }\n+\n+    #[test]\n+    fn off_disables_reasoning_rather_than_asking_for_a_little() {\n+        // The escape hatch. A model whose reasoning eats the answer is not\n+        // fixed by asking it to think less — `kimi-k3` ignored a 4096-token cap\n+        // and ran to 37k characters — so \"off\" has to mean off.\n+        assert_eq!(\n+            reasoning_options(\"off\"),\n+            json!({ \"reasoning\": { \"enabled\": false } })\n+        );\n+        assert_eq!(\n+            reasoning_options(\"\"),\n+            json!({ \"reasoning\": { \"enabled\": false } })\n+        );\n+    }\n+\n+    #[test]\n+    fn an_effort_is_passed_through_as_an_effort() {\n+        assert_eq!(\n+            reasoning_options(\"high\"),\n+            json!({ \"reasoning\": { \"effort\": \"high\" } })\n+        );\n+    }\n+\n+    #[test]\n+    fn the_configured_effort_reaches_the_gateway() {\n+        // `reasoning_options` is tested in isolation above; this asserts the\n+        // wire between config and gateway, which is the half that would leave\n+        // the setting inert — the same failure `max_tokens` had, where the\n+        // value was read, validated, documented, and then never forwarded.\n+        let mut models = models();\n+        models.reasoning_effort = \"off\".into();\n+\n+        // SAFETY-adjacent: no env mutation. The key is read from a variable the\n+        // config names, so the test names one it sets nowhere and asserts the\n+        // error, then builds the gateway directly for the positive case.\n+        let gateway = GatewayModel {\n+            api_key: \"unused\".into(),\n+            base_url: models.base_url.clone(),\n+            fallbacks: models.fallback.clone(),\n+            reasoning_effort: models.reasoning_effort.clone(),\n+        };\n+\n+        assert_eq!(\n+            reasoning_options(&gateway.reasoning_effort),\n+            json!({ \"reasoning\": { \"enabled\": false } })\n+        );\n+    }\n }\n--- a/src/harness/pricing.rs\n+++ b/src/harness/pricing.rs\n@@ -41,11 +41,52 @@ pub struct Price {\n     pub cached: f64,\n }\n \n-/// Per-million-token prices, verified against openrouter.ai on 2026-08-08.\n+/// Per-million-token prices, verified against openrouter.ai on 2026-08-09.\n ///\n /// tinyagents reports tokens but not cost, and the budget ceiling is\n /// denominated in dollars, so the conversion happens here.\n+/// Entries exist for models this deployment does not currently select. That is\n+/// deliberate: `completion_cost` fails closed on an unpriced model, so a\n+/// one-line change to `models.deep` would otherwise take the budget ceiling\n+/// with it. Every model here was measured before being priced.\n const MODEL_PRICES: &[(&str, Price)] = &[\n+    (\n+        // Not selected. Evaluated as a deep tier and rejected: cheaper per\n+        // token than kimi-k3 but it reasons 3-6x more, which spends the\n+        // difference — $0.00598 a call against $0.00556.\n+        \"qwen/qwen3.8-max\",\n+        Price {\n+            input: 2.00,\n+            output: 6.00,\n+            cached: 0.25,\n+        },\n+    ),\n+    (\n+        \"z-ai/glm-5.2\",\n+        Price {\n+            input: 0.07,\n+            output: 0.22,\n+            cached: 0.013,\n+        },\n+    ),\n+    (\n+        \"deepseek/deepseek-v4-pro\",\n+        Price {\n+            input: 0.435,\n+            output: 0.87,\n+            // Cache reads are a *hundredth* of the input price here, against a\n+            // tenth on kimi-k3. On a re-review that is most of the bill.\n+            cached: 0.003625,\n+        },\n+    ),\n+    (\n+        \"deepseek/deepseek-v4-flash\",\n+        Price {\n+            input: 0.14,\n+            output: 0.28,\n+            cached: 0.028,\n+        },\n+    ),\n     (\n         \"moonshotai/kimi-k3\",\n         Price {\n@@ -296,4 +337,71 @@ mod tests {\n         assert_eq!(estimate_tokens(\"abcd\"), 1);\n         assert_eq!(estimate_tokens(\"abcde\"), 2);\n     }\n+\n+    #[test]\n+    fn every_model_the_defaults_select_has_a_price() {\n+        // The test that matters more than any individual row. `completion_cost`\n+        // fails closed on an unpriced model, so shipping a `defaults.toml` that\n+        // names one turns the budget ceiling into a hard error on the first\n+        // review — and the failure would land in production, not here.\n+        let config: crate::config::types::Config = crate::config::DEFAULTS\n+            .parse::<toml::Table>()\n+            .expect(\"defaults parse\")\n+            .try_into()\n+            .expect(\"defaults deserialize\");\n+\n+        let mut named = vec![config.models.scan.clone(), config.models.deep.clone()];\n+        named.extend(config.models.fallback.iter().cloned());\n+\n+        let missing = unpriced(named.iter().map(String::as_str));\n+        assert!(\n+            missing.is_empty(),\n+            \"unpriced models in defaults: {missing:?}\"\n+        );\n+    }\n+\n+    #[test]\n+    fn the_new_deep_tier_costs_what_the_table_says() {\n+        // Anchored on the real figure this change was made against: PR #62's\n+        // security lane sent 82,914 input and 842 output tokens and cost\n+        // $0.2614 on the outgoing model. The same call on this one is the whole\n+        // argument for the switch, so it is pinned rather than asserted vaguely.\n+        // Note the argument order: input, **cached**, output. Getting it wrong\n+        // is silent — every argument is a `u64` — and I did exactly that while\n+        // writing this test, which is why the ordering is now pinned below.\n+        let cost = completion_cost(\"z-ai/glm-5.2\", 82_914, 0, 842);\n+\n+        assert!(\n+            (0.005..0.007).contains(&cost),\n+            \"expected roughly $0.006 for the 83k-token security call, got {cost:.5}\"\n+        );\n+    }\n+\n+    #[test]\n+    fn a_cached_read_is_charged_at_the_cache_rate() {\n+        // GLM 5.2 reads cache at a fifth of its input price, and a re-review is\n+        // mostly cache. Charging cached tokens at full price would misreport\n+        // exactly the number this change exists to move.\n+        let cold = completion_cost(\"z-ai/glm-5.2\", 100_000, 0, 1_000);\n+        let warm = completion_cost(\"z-ai/glm-5.2\", 100_000, 90_000, 1_000);\n+\n+        assert!(\n+            warm < cold,\n+            \"cached tokens must cost less: {warm} vs {cold}\"\n+        );\n+    }\n+\n+    #[test]\n+    fn the_middle_argument_is_cached_tokens_not_output() {\n+        // Every parameter is a `u64`, so swapping two of them compiles and\n+        // silently misreports the bill. Output is priced far above cache reads,\n+        // so the two orderings are distinguishable — which is what this asserts.\n+        let correct = completion_cost(\"z-ai/glm-5.2\", 1_000_000, 0, 1_000_000);\n+        let swapped = completion_cost(\"z-ai/glm-5.2\", 1_000_000, 1_000_000, 0);\n+\n+        assert!(\n+            correct > swapped,\n+            \"input+output must cost more than input-all-cached: {correct} vs {swapped}\"\n+        );\n+    }\n }\n"
    }
  ],
  "comments": [
    {
      "id": 5231332076,
      "author": "coderabbitai[bot]",
      "body": "<!-- This is an auto-generated comment: summarize by coderabbit.ai -->\n<!-- review_stack_entry_start -->\n\n[![Review Change Stack](https://storage.googleapis.com/coderabbit_public_assets/review-stack-in-coderabbit-ui.svg)](https://app.coderabbit.ai/change-stack/tinyhumansai/tinysweeper/pull/68?utm_source=github_walkthrough&utm_medium=github&utm_campaign=change_stack)\n\n<!-- review_stack_entry_end -->\n<!-- This is an auto-generated comment: rate limited by coderabbit.ai -->\n\n> [!WARNING]\n> ## Review limit reached\n> \n> `@senamakel`, you've reached your PR review limit, so we couldn't start this review.\n> \n> **Next review available in:** **12 minutes**\n> \n> You've used all free OSS reviews for now. Wait for the free limit to reset to keep reviewing this public repository.\n> \n> <details>\n> <summary>How can I continue?</summary>\n> \n> After more reviews become available, a review can be triggered using the `@coderabbitai review` command as a PR comment. Alternatively, push new commits to this PR.\n> \n> To avoid repeated limits, reduce automatic review volume by pausing incremental auto-reviews earlier, using label-based review opt-in, excluding WIP or generated PR titles, or requesting reviews manually when the PR is ready. If your team needs uninterrupted high-volume reviews, an organization admin can enable usage-based reviews.\n> \n> </details>\n> \n> \n> <details>\n> <summary>How do review limits work?</summary>\n> \n> CodeRabbit enforces per-developer PR review limits for each organization. Most developers receive the normal plan review availability.\n> \n> For paid Pro and Pro+ PR reviews, CodeRabbit uses adaptive limits for sustained high-volume activity. When a developer's recent PR review activity reaches the 95th percentile or higher among CodeRabbit users, additional reviews become available more gradually as earlier reviews age out of the rolling window.\n> \n> Please refer [docs](https://docs.coderabbit.ai/management/plans#rate-limits) for additional details.\n> \n> </details>\n> \n> <details>\n> <summary>Review details</summary>\n> \n> <details>\n> <summary>⚙️ Run configuration</summary>\n> \n> **Configuration used**: Organization UI\n> \n> **Review profile**: CHILL\n> \n> **Plan**: Pro Plus\n> \n> **Run ID**: `7598537e-7672-4b6b-8518-44f4fb683f92`\n> \n> </details>\n> \n> <details>\n> <summary>📥 Commits</summary>\n> \n> Reviewing files that changed from the base of the PR and between eb00c671b2f654f75f523d5c7b6861f79e4d0b60 and f57624fe521b3596c4be81f6b62d20bcc0d2a4aa.\n> \n> </details>\n> \n> <details>\n> <summary>📒 Files selected for processing (3)</summary>\n> \n> * `src/config/defaults.toml`\n> * `src/harness/openrouter.rs`\n> * `src/harness/pricing.rs`\n> \n> </details>\n> \n> </details>\n\n<!-- end of auto-generated comment: rate limited by coderabbit.ai -->\n\n<!-- walkthrough_start -->\n\n<details>\n<summary>📝 Walkthrough</summary>\n\n## Walkthrough\n\nThe PR adds configurable reasoning effort, changes default and fallback models to DeepSeek and Minimax variants, updates OpenRouter request handling and tests, and adds pricing metadata for DeepSeek V4 models.\n\n### Changes\n\n**Model Configuration and Gateway Integration**\n\n|Layer / File(s)|Summary|\n|---|---|\n|**Model defaults and pricing** <br> `src/config/defaults.toml`, `src/config/types.rs`, `src/harness/pricing.rs`|Defaults now select DeepSeek V4 models and enable high reasoning effort. `Models` exposes `reasoning_effort`. Pricing records include DeepSeek V4 Pro and Flash.|\n|**OpenRouter reasoning options** <br> `src/harness/openrouter.rs`|`GatewayModel` passes configured reasoning effort to OpenRouter. Empty and `off` values disable reasoning. Tests cover disabled and enabled options.|\n\n**Estimated code review effort:** 3 (Moderate) | ~20 minutes\n\n### Sequence Diagram(s)\n\n```mermaid\nsequenceDiagram\n  participant Models\n  participant GatewayModel\n  participant OpenRouter\n  Models->>GatewayModel: Load reasoning_effort\n  GatewayModel->>OpenRouter: Send reasoning_options\n  OpenRouter-->>GatewayModel: Return model response\n```\n\n**Poem**\n\n> I’m a rabbit with models bright,  \n> DeepSeek hops into the night.  \n> Reasoning follows the chosen way,  \n> Prices keep the ledger gray.  \n> Tests thump softly: “All is right!”\n\n</details>\n\n<!-- walkthrough_end -->\n<!-- pre_merge_checks_walkthrough_start -->\n\n<details>\n<summary>🚥 Pre-merge checks | ✅ 4 | ❌ 1</summary>\n\n### ❌ Failed checks (1 warning)\n\n|  Check name | Status     | Explanation                                                                                                                           | Resolution                                                                             |\n| :---------: | :--------- | :------------------------------------------------------------------------------------------------------------------------------------ | :------------------------------------------------------------------------------------- |\n| Title check | ⚠️ Warning | The title mentions GLM 5.2, but the changes update models to DeepSeek V4; only the configurable reasoning part matches the changeset. | Rename the title to describe the DeepSeek V4 model changes and configurable reasoning. |\n\n<details>\n<summary>✅ Passed checks (4 passed)</summary>\n\n|         Check name         | Status   | Explanation                                                                                                |\n| :------------------------: | :------- | :--------------------------------------------------------------------------------------------------------- |\n|     Docstring Coverage     | ✅ Passed | No functions found in the changed files to evaluate docstring coverage. Skipping docstring coverage check. |\n|     Linked Issues check    | ✅ Passed | Check skipped because no linked issues were found for this pull request.                                   |\n| Out of Scope Changes check | ✅ Passed | Check skipped because no linked issues were found for this pull request.                                   |\n|      Description Check     | ✅ Passed | Check skipped - CodeRabbit’s high-level summary is enabled.                                                |\n\n</details>\n\n</details>\n\n<!-- pre_merge_checks_walkthrough_end -->\n<!-- finishing_touch_checkbox_start -->\n\n<details>\n<summary>✨ Finishing Touches</summary>\n\n<details>\n<summary>📝 Generate docstrings</summary>\n\n- [ ] <!-- {\"checkboxId\": \"7962f53c-55bc-4827-bfbf-6a18da830691\"} --> Create stacked PR\n- [ ] <!-- {\"checkboxId\": \"3e1879ae-f29b-4d0d-8e06-d12b7ba33d98\"} --> Commit on current branch\n\n</details>\n\n</details>\n\n<!-- finishing_touch_checkbox_end -->\n<!-- tips_start -->\n\n---\n\nThanks for using [CodeRabbit](https://coderabbit.ai?utm_source=oss&utm_medium=github&utm_campaign=tinyhumansai/tinysweeper&utm_content=68)! It's free for OSS, and your support helps us grow. If you like it, consider giving us a shout-out.\n\n<details>\n<summary>❤️ Share</summary>\n\n- [X](https://twitter.com/intent/tweet?text=I%20just%20used%20%40coderabbitai%20for%20my%20code%20review%2C%20and%20it%27s%20fantastic%21%20It%27s%20free%20for%20OSS%20and%20offers%20a%20free%20trial%20for%20the%20proprietary%20code.%20Check%20it%20out%3A&url=https%3A//coderabbit.ai)\n- [Mastodon](https://mastodon.social/share?text=I%20just%20used%20%40coderabbitai%20for%20my%20code%20review%2C%20and%20it%27s%20fantastic%21%20It%27s%20free%20for%20OSS%20and%20offers%20a%20free%20trial%20for%20the%20proprietary%20code.%20Check%20it%20out%3A%20https%3A%2F%2Fcoderabbit.ai)\n- [Reddit](https://www.reddit.com/submit?title=Great%20tool%20for%20code%20review%20-%20CodeRabbit&text=I%20just%20used%20CodeRabbit%20for%20my%20code%20review%2C%20and%20it%27s%20fantastic%21%20It%27s%20free%20for%20OSS%20and%20offers%20a%20free%20trial%20for%20proprietary%20code.%20Check%20it%20out%3A%20https%3A//coderabbit.ai)\n- [LinkedIn](https://www.linkedin.com/sharing/share-offsite/?url=https%3A%2F%2Fcoderabbit.ai&mini=true&title=Great%20tool%20for%20code%20review%20-%20CodeRabbit&summary=I%20just%20used%20CodeRabbit%20for%20my%20code%20review%2C%20and%20it%27s%20fantastic%21%20It%27s%20free%20for%20OSS%20and%20offers%20a%20free%20trial%20for%20proprietary%20code)\n\n</details>\n\n\n<sub>Comment `@coderabbitai help` to get the list of available commands.</sub>\n\n<!-- tips_end -->"
    },
    {
      "id": 5231490472,
      "author": "senamakel",
      "body": "The `description` lane read a body I had already replaced — I pushed the commit and edited the body a minute later, so the webhook fired on the old text. Against what it saw, all three findings were correct.\n\nThe third one survives the body update and was worth fixing: **`qwen/qwen3.8-max` had a price entry and no basis** — nothing selects it.\n\nRather than delete it, I stated why unused entries exist, because the reason is load-bearing:\n\n> Entries exist for models this deployment does not currently select. That is deliberate: `completion_cost` fails closed on an unpriced model, so a one-line change to `models.deep` would otherwise take the budget ceiling with it. Every model here was measured before being priced.\n\nEach unused entry now carries its verdict too — `qwen3.8-max` is marked as evaluated and rejected, with the measurement that decided it ($0.00598 a call against `kimi-k3`'s $0.00556, because it reasons 3–6× more).\n\nThe first two findings are the stale-body ones. The body now matches the diff: GLM 5.2 on both tiers, priced and measured, with the fallback chain as written.\n\nWorth noting what happened here in general terms: an accurate reviewer plus a body edited after the push produces a confident, well-argued, wrong review. The lane is doing its job — a diff-versus-description check is exactly the kind of thing that catches a body someone forgot to update — it just cannot see an edit that arrives after it ran."
    },
    {
      "id": 5231525935,
      "author": "senamakel",
      "body": "Both findings were right, and acting on the first one **found a bug in the test I was writing to satisfy it** — which is the best possible outcome for a \"this is untested\" finding.\n\n`completion_cost(model, input, cached, output)`. I called it `(model, input, output, cached)`. Every parameter is a `u64`, so it compiled, ran, and silently misreported the bill — the cached call came out **four times more expensive** than the cold one. That is exactly the failure mode the price table exists to prevent, and nothing would have caught it.\n\nSo the ordering is now pinned by a test of its own, `the_middle_argument_is_cached_tokens_not_output`, which works because output is priced far above cache reads and the two orderings are therefore distinguishable.\n\nFive tests added:\n\n- **`every_model_the_defaults_select_has_a_price`** — the one that matters most. It parses the shipped `defaults.toml` and asserts `scan`, `deep` and every fallback are priced. `completion_cost` fails closed on an unpriced model, so naming one in defaults turns the budget ceiling into a hard error on the first review of the day — in production, not in CI. This is now a compile-and-test-time failure instead.\n- **`the_new_deep_tier_costs_what_the_table_says`** — anchored on the real figure this change was made against: PR #62's security lane, 82,914 input and 842 output tokens, $0.2614 on the outgoing model. Asserts ~$0.006 here.\n- **`a_cached_read_is_charged_at_the_cache_rate`** — a re-review is mostly cache, and charging it at full input price would misreport precisely the number this change exists to move.\n- **`the_middle_argument_is_cached_tokens_not_output`** — above.\n- **`the_configured_effort_reaches_the_gateway`** — the second finding. Asserts the wire between config and gateway rather than just `reasoning_options` in isolation, because that is the half that leaves a setting inert. `max_tokens` had exactly this bug: read, validated, documented, never forwarded.\n\n985 passing, 1,127 with all features."
    },
    {
      "id": 5231553594,
      "author": "senamakel",
      "body": "Pushing back on the `description` finding: **the mismatch it reports does not exist.**\n\nBody:\n\n```\n| `fallback` | `kimi-k2.7-code`, `minimax-m2.1` | `deepseek-v4-pro`, `minimax-m3` |\n```\n\nDiff:\n\n```\n+fallback = [\"deepseek/deepseek-v4-pro\", \"minimax/minimax-m3\"]\n```\n\nThose agree. The title is `perf(models): move to GLM 5.2, and make reasoning configurable`, and the diff sets both tiers to `z-ai/glm-5.2`. Those agree too.\n\nThis was a real finding two pushes ago, when I edited the body after pushing and the lane reviewed the old text. It has been fixed since, and the summary itself says **\"(5 earlier finding(s) still open)\"** — so the lane is not re-deriving it, it is **carrying it forward**.\n\nThat is worth treating as a defect rather than noise, because it is the failure mode the continuity design is most exposed to: prior findings are replayed into the prompt so a re-review stays cheap and consistent, and the risk is that the model agrees with them instead of re-checking them against the current diff. A finding that cannot be retired by fixing it is worse than one that was never raised — it trains everyone to ignore the lane.\n\nFiling it separately. The `tests` lane, by contrast, correctly noticed its earlier findings were addressed and said so in its summary — so the retirement path works there, which makes the `description` behaviour a bug rather than a missing feature.\n\nMerging on the strength of the evidence above."
    }
  ],
  "blobs": {
    "AGENTS.md": "# Repository Guidelines\n\n## Project Structure & Module Organization\n\n- `src/` — the single crate. One responsibility per module, core types in a\n  module-local `types.rs`, and every port in `src/ports/` is one trait in one\n  file.\n- `src/bin/tinysweeper.rs` — the CLI. Every subcommand is declared even when its\n  milestone has not landed, so scripts and runbooks can be written against a\n  stable surface. `src/server/` is the only production surface: tinysweeper is\n  not distributed or run as a GitHub Action. The single, deliberate exception is\n  `.github/workflows/manual-review.yml` in *this* repository — an operator\n  button that POSTs to the deployed server's `/admin/reviews` route. It builds\n  nothing, runs no lane, and holds no model or write credential; anything that\n  would need one belongs in `src/server/`, not in a workflow.\n- `presets/` — review policy as **data**, not code. A preset is a folder with a\n  `preset.toml`, a `README.md`, and optional prompt overrides. Adding a preset\n  is a new folder, never a new module.\n- `vendor/tinyagents` — the agent harness, as a git submodule. Never edit it\n  here; change it upstream and bump the pin.\n- `docs/modules/<module>/README.md` — one document per `src/` module.\n- `examples/` — declared explicitly in `Cargo.toml` with `required-features`, so\n  credential-needing smoke tests never build in CI.\n\n## Build, Test, and Development Commands\n\n```sh\ngit submodule update --init --recursive\ncargo fmt --all -- --check\ncargo clippy --locked --all-targets -- -D warnings\ncargo test --locked\ncargo check --locked --all-features --all-targets\n```\n\nThe default build is offline: it links no HTTP client and the test suite never\ntouches the network. Anything that needs the network goes behind a feature\n(`harness`, `github`, `serve`).\n\n## Coding Style & Naming Conventions\n\n- rustfmt output, Rust 2024 idioms. `snake_case` modules and files,\n  `PascalCase` types.\n- Return `Result<T>` using the crate error type from `src/error.rs`.\n- Every file opens with a `//!` module doc describing its role and any feature\n  gating.\n- Comments explain the *decision*, not the code. When a flag or an ordering is\n  load-bearing, say so and say why.\n- Public items carry doc comments. Clap fields use `///` — it becomes the help\n  text.\n\n## Testing Guidelines\n\n- Tests live **in-crate**: a `#[cfg(test)] mod tests` block at the bottom of the\n  module, moving to a sibling `test.rs` or `<name>_test.rs` when they grow.\n  There is no `tests/` directory.\n- Every port has an always-compiled offline mock. `MockForge` records what would\n  have been written so tests can assert on exact check runs and comments.\n- Lane behaviour is covered by golden tests: fixture diff plus a canned\n  structured model response, asserting the findings that survive filtering,\n  dedupe and capping. These are the tests that keep noise control honest.\n- Maintain at least 80% coverage for meaningful library behaviour.\n\n## Documentation Expectations\n\nKeep every Markdown file, including this one, at 500 lines or fewer. When a\ntopic grows past that limit, split it into focused files and link them from the\nmodule's `README.md`.\n\n## Security Boundary\n\nThese are invariants, not preferences. Changing any of them needs an explicit\ndiscussion in the pull request:\n\n- The model never holds a write token. Lanes run against a read-only checkout;\n  write credentials are minted only in `src/apply/`, after every model call has\n  returned. A lane that leaves a change in the checkout fails.\n- Contributor code is never executed. We read the diff and the tree; we do not\n  build, install dependencies, or run the target repository's scripts.\n- Pull request bodies, comments and diffs are untrusted input. Fence and label\n  them as data in prompts. A model verdict is advisory — only deterministic\n  policy in `src/apply/` and `src/automerge/` may mutate GitHub.\n- Secrets found by the scanners are reported by type and location only. The\n  value never reaches a comment, a check-run summary, or a log.\n\n## Commit & Pull Request Guidelines\n\nUse concise, imperative commit subjects. Keep commits small; commit each\ncoherent, validated slice on its own. Pull requests state what changed, any\nbehaviour change, and how it was verified.\n",
    "CLAUDE.md": "# Repository Guidelines\n\n## Project Structure & Module Organization\n\n- `src/` — the single crate. One responsibility per module, core types in a\n  module-local `types.rs`, and every port in `src/ports/` is one trait in one\n  file.\n- `src/bin/tinysweeper.rs` — the CLI. Every subcommand is declared even when its\n  milestone has not landed, so scripts and runbooks can be written against a\n  stable surface. `src/server/` is the only production surface: tinysweeper is\n  not distributed or run as a GitHub Action. The single, deliberate exception is\n  `.github/workflows/manual-review.yml` in *this* repository — an operator\n  button that POSTs to the deployed server's `/admin/reviews` route. It builds\n  nothing, runs no lane, and holds no model or write credential; anything that\n  would need one belongs in `src/server/`, not in a workflow.\n- `presets/` — review policy as **data**, not code. A preset is a folder with a\n  `preset.toml`, a `README.md`, and optional prompt overrides. Adding a preset\n  is a new folder, never a new module.\n- `vendor/tinyagents` — the agent harness, as a git submodule. Never edit it\n  here; change it upstream and bump the pin.\n- `docs/modules/<module>/README.md` — one document per `src/` module.\n- `examples/` — declared explicitly in `Cargo.toml` with `required-features`, so\n  credential-needing smoke tests never build in CI.\n\n## Build, Test, and Development Commands\n\n```sh\ngit submodule update --init --recursive\ncargo fmt --all -- --check\ncargo clippy --locked --all-targets -- -D warnings\ncargo test --locked\ncargo check --locked --all-features --all-targets\n```\n\nThe default build is offline: it links no HTTP client and the test suite never\ntouches the network. Anything that needs the network goes behind a feature\n(`harness`, `github`, `serve`).\n\n## Coding Style & Naming Conventions\n\n- rustfmt output, Rust 2024 idioms. `snake_case` modules and files,\n  `PascalCase` types.\n- Return `Result<T>` using the crate error type from `src/error.rs`.\n- Every file opens with a `//!` module doc describing its role and any feature\n  gating.\n- Comments explain the *decision*, not the code. When a flag or an ordering is\n  load-bearing, say so and say why.\n- Public items carry doc comments. Clap fields use `///` — it becomes the help\n  text.\n\n## Testing Guidelines\n\n- Tests live **in-crate**: a `#[cfg(test)] mod tests` block at the bottom of the\n  module, moving to a sibling `test.rs` or `<name>_test.rs` when they grow.\n  There is no `tests/` directory.\n- Every port has an always-compiled offline mock. `MockForge` records what would\n  have been written so tests can assert on exact check runs and comments.\n- Lane behaviour is covered by golden tests: fixture diff plus a canned\n  structured model response, asserting the findings that survive filtering,\n  dedupe and capping. These are the tests that keep noise control honest.\n- Maintain at least 80% coverage for meaningful library behaviour.\n\n## Documentation Expectations\n\nKeep every Markdown file, including this one, at 500 lines or fewer. When a\ntopic grows past that limit, split it into focused files and link them from the\nmodule's `README.md`.\n\n## Security Boundary\n\nThese are invariants, not preferences. Changing any of them needs an explicit\ndiscussion in the pull request:\n\n- The model never holds a write token. Lanes run against a read-only checkout;\n  write credentials are minted only in `src/apply/`, after every model call has\n  returned. A lane that leaves a change in the checkout fails.\n- Contributor code is never executed. We read the diff and the tree; we do not\n  build, install dependencies, or run the target repository's scripts.\n- Pull request bodies, comments and diffs are untrusted input. Fence and label\n  them as data in prompts. A model verdict is advisory — only deterministic\n  policy in `src/apply/` and `src/automerge/` may mutate GitHub.\n- Secrets found by the scanners are reported by type and location only. The\n  value never reaches a comment, a check-run summary, or a log.\n\n## Commit & Pull Request Guidelines\n\nUse concise, imperative commit subjects. Keep commits small; commit each\ncoherent, validated slice on its own. Pull requests state what changed, any\nbehaviour change, and how it was verified.\n"
  },
  "lookups": {
    "read crates/opencompany-app/src/proxy/mod.rs:178-206": {
      "outcome": "text",
      "text": "  178| /// The console turns *any* rejection into its own `network_error` — the\n  179| /// transport's own words are deliberately not surfaced, because they differ per\n  180| /// transport and the message is shown to a person. So there is nothing to\n  181| /// structure here.\n  182| #[derive(Debug)]\n  183| pub struct ProxyErrorWire(pub String);\n  184| \n  185| impl From<ProxyError> for ProxyErrorWire {\n  186|     fn from(error: ProxyError) -> Self {\n  187|         Self(error.to_string())\n  188|     }\n  189| }\n  190| \n  191| /// Every host this client is connected to.\n  192| #[derive(Debug)]\n  193| pub struct ProxyRegistry {\n  194|     connections: RwLock<HashMap<ConnectionId, Connection>>,\n  195|     http: reqwest::Client,\n  196| }\n  197| \n  198| impl Default for ProxyRegistry {\n  199|     fn default() -> Self {\n  200|         Self {\n  201|             connections: RwLock::new(HashMap::new()),\n  202|             http: reqwest::Client::builder()\n  203|                 .redirect(reqwest::redirect::Policy::none())\n  204|                 // A host that accepts a connection but never answers must not\n  205|                 // hold a console command open forever. Deliberately *not* a\n  206|                 // client-wide `timeout`: that would also cut the long-lived",
      "start": 178,
      "end": 206,
      "total": 925
    },
    "read crates/opencompany-core/src/app/config.rs:464-492": {
      "outcome": "text",
      "text": "  464| fn gib_to_bytes(gb: Option<f64>) -> Option<u64> {\n  465|     gb.filter(|g| *g > 0.0)\n  466|         .map(|g| (g * 1024.0 * 1024.0 * 1024.0) as u64)\n  467| }\n  468| \n  469| /// Resolved `[workspace]` configuration.\n  470| #[derive(Clone, Debug)]\n  471| pub struct WorkspaceConfig {\n  472|     /// Whether private agent workspaces keep automatic Git checkpoints.\n  473|     pub git_enabled: bool,\n  474|     /// Whether the ephemeral `tmp/` scratch is cleared on startup.\n  475|     pub clear_tmp_on_startup: bool,\n  476|     /// Soft whole-workspace quota in bytes; `None` is unlimited.\n  477|     pub storage_quota_bytes: Option<u64>,\n  478|     /// Soft `tmp/` quota in bytes; `None` is unlimited.\n  479|     pub tmp_quota_bytes: Option<u64>,\n  480|     /// The workspace tree's **enforced** byte limits (issue #553).\n  481|     pub quota: crate::runtime::WorkspaceQuota,\n  482| }\n  483| \n  484| impl Default for WorkspaceConfig {\n  485|     fn default() -> Self {\n  486|         Self {\n  487|             git_enabled: false,\n  488|             clear_tmp_on_startup: true,\n  489|             storage_quota_bytes: None,\n  490|             tmp_quota_bytes: None,\n  491|             quota: crate::runtime::WorkspaceQuota::default(),\n  492|         }",
      "start": 464,
      "end": 492,
      "total": 2251
    },
    "read crates/opencompany-core/src/app/types.rs:109-137": {
      "outcome": "text",
      "text": "  109|     /// and does not delete an account it already created. `None` — and an empty\n  110|     /// or whitespace-only value — is a clean no-op.\n  111|     pub admin_email: Option<String>,\n  112|     /// A host-wide override of every company's `[users].mode`\n  113|     /// (`OPENCOMPANY_AUTH_MODE`, or `auth_mode` in `config.toml`).\n  114|     ///\n  115|     /// `None` — the normal case — leaves each company to name its own sign-in\n  116|     /// mode in its manifest. It exists because two deployments own the answer\n  117|     /// rather than the company definition does: the packaged desktop app, which\n  118|     /// forces [`AuthMode::None`](crate::app::config::AuthMode::None) because\n  119|     /// there is only ever the person at the machine, and a hosting platform,\n  120|     /// which must be able to guarantee a mode across every tenant it builds\n  121|     /// regardless of what a tenant wrote.\n  122|     ///\n  123|     /// Carried to each company by\n  124|     /// [`RuntimeBuilder::with_auth_mode_override`](crate::runtime::RuntimeBuilder::with_auth_mode_override),\n  125|     /// which is where it beats the manifest.\n  126|     pub auth_mode_override: Option<AuthMode>,\n  127| }\n  128| \n  129| impl Default for AppConfig {\n  130|     fn default() -> Self {\n  131|         Self {\n  132|             bind: \"127.0.0.1:8080\".to_string(),\n  133|             openhuman_root: None,\n  134|             api_url: crate::app::config::DEFAULT_API_URL.to_string(),\n  135|             web_url: None,\n  136|             instance_name: None,\n  137|             brain_mode: BrainMode::Hosted,",
      "start": 109,
      "end": 137,
      "total": 1838
    },
    "read crates/opencompany-core/src/company/inference/store.rs:880-908": {
      "outcome": "text",
      "text": "  880|     enabled: bool,\n  881| ) -> Result<()> {\n  882|     secrets\n  883|         .set(\n  884|             company,\n  885|             MANAGED_ENABLED_KEY,\n  886|             SecretValue(if enabled { \"true\" } else { \"false\" }.to_string()),\n  887|         )\n  888|         .await\n  889| }\n  890| \n  891| // ---- the default provider ---------------------------------------------------\n  892| \n  893| /// The [`SecretStore`] key naming the company's default provider.\n  894| pub const DEFAULT_PROVIDER_KEY: &str = \"inference/default\";\n  895| \n  896| /// A provider slug and the one model to send it (keys rework, issue #2306,\n  897| /// slice 2b): the only shape the new resolution path sends. Serialized field\n  898| /// order is `provider`, then `model`.\n  899| #[derive(Clone, Debug, PartialEq, Eq, Serialize, Deserialize)]\n  900| pub struct ModelChoice {\n  901|     /// A slug from [`list_providers`], which includes entry zero.\n  902|     pub provider: String,\n  903|     /// The id that provider's API accepts. Non-empty after trim.\n  904|     pub model: String,\n  905| }\n  906| \n  907| /// `inference/default`, parsed (keys rework, issue #2306, slice 2b).\n  908| #[derive(Clone, Debug, PartialEq, Eq)]",
      "start": 880,
      "end": 908,
      "total": 2291
    },
    "read crates/opencompany-core/src/metering/model.rs:73-101": {
      "outcome": "text",
      "text": "   73| //! [`model_for_tier`]: crate::company::inference::model_for_tier\n   74| //! [`DEFAULT_TIER_MODELS`]: crate::company::inference::DEFAULT_TIER_MODELS\n   75| //! [`UsageSample`]: crate::ports::usage::UsageSample\n   76| \n   77| use std::fmt;\n   78| \n   79| use serde::{Deserialize, Deserializer, Serialize, Serializer};\n   80| \n   81| /// A model identity folded onto the closed vocabulary described in the\n   82| /// [module docs](self).\n   83| ///\n   84| /// The inner value is a private `&'static str`, so a `ModelSlug` **cannot** be\n   85| /// constructed from runtime data except through [`ModelSlug::classify`], which\n   86| /// only ever returns a compiled-in literal. That is what makes \"the raw model\n   87| /// name never leaves the harness\" a property of the type rather than a\n   88| /// convention a call site has to keep remembering — and it is the same\n   89| /// construction argument the analytics payload vocabulary in #1751 makes, so\n   90| /// [`as_str`](ModelSlug::as_str) drops straight into a `&'static str` property\n   91| /// with no second classifier.\n   92| #[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord)]\n   93| pub struct ModelSlug(&'static str);\n   94| \n   95| impl ModelSlug {\n   96|     /// A model ran, but it is not one this build can name — a BYOK or\n   97|     /// self-hosted model, or a vendor the vocabulary has not been extended to.\n   98|     ///\n   99|     /// Deliberately not \"unknown\": nothing is unknown here. The endpoint was\n  100|     /// asked for a specific model and answered; what is missing is a *name for\n  101|     /// it that is safe to keep*, which is a different fact.",
      "start": 73,
      "end": 101,
      "total": 411
    },
    "read crates/opencompany-core/src/server/inference_models.rs:436-464": {
      "outcome": "text",
      "text": "  436|         }\n  437|     }\n  438|     Ok(collector\n  439|         .finish()\n  440|         .into_iter()\n  441|         .map(|e| InferenceModel {\n  442|             id: e.id,\n  443|             name: e.name,\n  444|             context_length: e.context_length,\n  445|         })\n  446|         .collect())\n  447| }\n  448| \n  449| struct CacheEntry {\n  450|     at: Instant,\n  451|     models: Vec<InferenceModel>,\n  452| }\n  453| \n  454| /// One endpoint's catalog cache.\n  455| #[derive(Default)]\n  456| pub(crate) struct ModelCatalogCache {\n  457|     entry: Mutex<Option<CacheEntry>>,\n  458|     /// The last failure and when it happened — see [`MODEL_CATALOG_FAILURE_TTL`].\n  459|     failure: Mutex<Option<(Instant, String)>>,\n  460|     /// Serializes cache-miss fetches (issue #1838 follow-up). Held across the\n  461|     /// whole `discover_models` await, not just the cache write: without it,\n  462|     /// every console request that lands after startup or a TTL expiry sees\n  463|     /// the same empty/stale entry and fires its own upstream fetch, so a\n  464|     /// multi-tenant host can burst several identical registry calls at once —",
      "start": 436,
      "end": 464,
      "total": 1676
    },
    "read src/config/types.rs:1-50": {
      "outcome": "not_found"
    },
    "read src/config/types.rs:370-410": {
      "outcome": "not_found"
    },
    "read src/harness/pricing.rs:1-160": {
      "outcome": "not_found"
    },
    "read vendor/openhuman/tests/raw_coverage/inference_local_admin_raw_coverage_e2e.rs:439-467": {
      "outcome": "text",
      "text": "  439|     headers: HeaderMap,\n  440|     Json(body): Json<Value>,\n  441| ) -> impl IntoResponse {\n  442|     remember(&state, \"/v1/responses\", &headers, body.clone());\n  443|     if body[\"input\"]\n  444|         .as_str()\n  445|         .map(str::trim)\n  446|         .unwrap_or_default()\n  447|         .is_empty()\n  448|     {\n  449|         return Json(json!({ \"output\": [] })).into_response();\n  450|     }\n  451|     Json(json!({\n  452|         \"output\": [{\n  453|             \"content\": [{ \"type\": \"output_text\", \"text\": \"responses fallback\" }]\n  454|         }]\n  455|     }))\n  456|     .into_response()\n  457| }\n  458| \n  459| async fn models(State(state): State<MockState>) -> impl IntoResponse {\n  460|     let models = state\n  461|         .ollama_models\n  462|         .lock()\n  463|         .expect(\"models\")\n  464|         .iter()\n  465|         .map(|id| json!({ \"id\": id, \"owned_by\": \"round16\", \"context_window\": 8192 }))\n  466|         .collect::<Vec<_>>();\n  467|     Json(json!({ \"object\": \"list\", \"data\": models }))",
      "start": 439,
      "end": 467,
      "total": 628
    },
    "read vendor/openhuman/tests/raw_coverage/inference_provider_raw_coverage_e2e.rs:466-494": {
      "outcome": "text",
      "text": "  466|             \"usage\": {\n  467|                 \"prompt_tokens\": 5,\n  468|                 \"completion_tokens\": 6,\n  469|                 \"prompt_tokens_details\": { \"cached_tokens\": 2 }\n  470|             }\n  471|         })\n  472|         .to_string(),\n  473|     ];\n  474|     let body = chunks\n  475|         .into_iter()\n  476|         .map(|chunk| format!(\"data: {chunk}\\n\\n\"))\n  477|         .chain(std::iter::once(\"data: [DONE]\\n\\n\".to_string()))\n  478|         .collect::<String>();\n  479|     Response::builder()\n  480|         .status(StatusCode::OK)\n  481|         .header(header::CONTENT_TYPE, \"text/event-stream\")\n  482|         .body(Body::from(body))\n  483|         .expect(\"sse response\")\n  484| }\n  485| \n  486| async fn models(State(state): State<MockState>, headers: HeaderMap) -> impl IntoResponse {\n  487|     remember(&state, \"/v1/models\", &headers, Value::Null);\n  488|     Json(json!({\n  489|         \"object\": \"list\",\n  490|         \"data\": [\n  491|             { \"id\": \"demo-chat\", \"owned_by\": \"test-suite\" },\n  492|             { \"id\": \"demo-coder\", \"owned_by\": \"test-suite\", \"context_window\": 8192 },\n  493|             { \"owned_by\": \"ignored-without-id\" }\n  494|         ]",
      "start": 466,
      "end": 494,
      "total": 587
    },
    "search \"enum GatewayModel\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"enum Price\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn a_cached_read_is_charged_at_the_cache_rate(\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn an_effort_is_passed_through_as_an_effort(\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn completion_cost\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn completion_cost(\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn every_model_the_defaults_select_has_a_price(\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn models(\"": {
      "outcome": "hits",
      "hits": [
        {
          "path": "vendor/openhuman/tests/raw_coverage/inference_local_admin_raw_coverage_e2e.rs",
          "line": 459,
          "text": "async fn models(State(state): State<MockState>) -> impl IntoResponse {"
        },
        {
          "path": "vendor/openhuman/tests/raw_coverage/inference_provider_raw_coverage_e2e.rs",
          "line": 486,
          "text": "async fn models(State(state): State<MockState>, headers: HeaderMap) -> impl IntoResponse {"
        }
      ],
      "truncated": false
    },
    "search \"fn off_disables_reasoning_rather_than_asking_for_a_little(\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn reasoning_options(\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn the_configured_effort_reaches_the_gateway(\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn the_middle_argument_is_cached_tokens_not_output(\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn the_new_deep_tier_costs_what_the_table_says(\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn unpriced\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"fn unpriced(\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"impl Default for ModelConfig\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"impl Default for\"": {
      "outcome": "hits",
      "hits": [
        {
          "path": "crates/opencompany-app/src/proxy/mod.rs",
          "line": 198,
          "text": "impl Default for ProxyRegistry {"
        },
        {
          "path": "crates/opencompany-core/src/app/config.rs",
          "line": 484,
          "text": "impl Default for WorkspaceConfig {"
        },
        {
          "path": "crates/opencompany-core/src/app/types.rs",
          "line": 129,
          "text": "impl Default for AppConfig {"
        },
        {
          "path": "crates/opencompany-core/src/brain/sidecar/mock.rs",
          "line": 224,
          "text": "impl Default for MockInferenceClient {"
        },
        {
          "path": "crates/opencompany-core/src/company/inference.rs",
          "line": 142,
          "text": "impl Default for HarnessScope {"
        },
        {
          "path": "crates/opencompany-core/src/company/inference/store.rs",
          "line": 129,
          "text": "impl Default for ProviderId {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1218,
          "text": "impl Default for Workflows {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1279,
          "text": "impl Default for Users {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1304,
          "text": "impl Default for Brain {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1622,
          "text": "impl Default for Tools {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1724,
          "text": "impl Default for Policy {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1816,
          "text": "impl Default for Plan {"
        },
        {
          "path": "crates/opencompany-core/src/economy/client.rs",
          "line": 239,
          "text": "impl Default for MockTinyplaceClient {"
        },
        {
          "path": "crates/opencompany-core/src/economy/siwx.rs",
          "line": 189,
          "text": "impl Default for NonceCache {"
        },
        {
          "path": "crates/opencompany-core/src/feedback/board.rs",
          "line": 148,
          "text": "impl Default for BoardQuery {"
        },
        {
          "path": "crates/opencompany-core/src/feedback/github.rs",
          "line": 114,
          "text": "impl Default for RateLimiter {"
        },
        {
          "path": "crates/opencompany-core/src/feedback/service.rs",
          "line": 57,
          "text": "impl Default for FeedbackFiler {"
        },
        {
          "path": "crates/opencompany-core/src/feedback/tinyhumans.rs",
          "line": 144,
          "text": "impl Default for MockTinyHumansClient {"
        },
        {
          "path": "crates/opencompany-core/src/feedback/triage.rs",
          "line": 411,
          "text": "impl Default for QualityLedger {"
        },
        {
          "path": "crates/opencompany-core/src/harness/built_in/mod.rs",
          "line": 2752,
          "text": "impl Default for HarnessPool {"
        },
        {
          "path": "crates/opencompany-core/src/harness/built_in/policy.rs",
          "line": 459,
          "text": "impl Default for ApprovalRequestQueue {"
        },
        {
          "path": "crates/opencompany-core/src/harness/built_in/provider.rs",
          "line": 1312,
          "text": "impl Default for MockProvider {"
        },
        {
          "path": "crates/opencompany-core/src/harness/pages_tools.rs",
          "line": 145,
          "text": "impl Default for PageManifest {"
        },
        {
          "path": "crates/opencompany-core/src/ports/brain.rs",
          "line": 95,
          "text": "impl Default for Cognition {"
        },
        {
          "path": "crates/opencompany-core/src/ports/types.rs",
          "line": 3312,
          "text": "impl Default for TurnStep {"
        },
        {
          "path": "crates/opencompany-core/src/ports/workflow_runner.rs",
          "line": 920,
          "text": "impl Default for RunCancel {"
        },
        {
          "path": "crates/opencompany-core/src/runtime/delegation.rs",
          "line": 106,
          "text": "impl Default for ChatTarget<'_> {"
        },
        {
          "path": "crates/opencompany-core/src/runtime/run_supervisor.rs",
          "line": 97,
          "text": "impl Default for RunSupervisor {"
        },
        {
          "path": "crates/opencompany-core/src/runtime/workspace_quota.rs",
          "line": 155,
          "text": "impl Default for WorkspaceQuota {"
        },
        {
          "path": "crates/opencompany-core/src/server/ops/mailer.rs",
          "line": 231,
          "text": "impl Default for RecordingMailReceiver {"
        }
      ],
      "truncated": true
    },
    "search \"impl Default for\" in src/config/types.rs": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"impl Default\"": {
      "outcome": "hits",
      "hits": [
        {
          "path": "crates/opencompany-app/src/proxy/mod.rs",
          "line": 198,
          "text": "impl Default for ProxyRegistry {"
        },
        {
          "path": "crates/opencompany-core/src/app/config.rs",
          "line": 484,
          "text": "impl Default for WorkspaceConfig {"
        },
        {
          "path": "crates/opencompany-core/src/app/types.rs",
          "line": 129,
          "text": "impl Default for AppConfig {"
        },
        {
          "path": "crates/opencompany-core/src/brain/sidecar/mock.rs",
          "line": 224,
          "text": "impl Default for MockInferenceClient {"
        },
        {
          "path": "crates/opencompany-core/src/company/inference.rs",
          "line": 142,
          "text": "impl Default for HarnessScope {"
        },
        {
          "path": "crates/opencompany-core/src/company/inference/store.rs",
          "line": 129,
          "text": "impl Default for ProviderId {"
        },
        {
          "path": "crates/opencompany-core/src/company/inference/store.rs",
          "line": 919,
          "text": "impl DefaultChoice {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1218,
          "text": "impl Default for Workflows {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1279,
          "text": "impl Default for Users {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1304,
          "text": "impl Default for Brain {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1622,
          "text": "impl Default for Tools {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1724,
          "text": "impl Default for Policy {"
        },
        {
          "path": "crates/opencompany-core/src/company/types.rs",
          "line": 1816,
          "text": "impl Default for Plan {"
        },
        {
          "path": "crates/opencompany-core/src/economy/client.rs",
          "line": 239,
          "text": "impl Default for MockTinyplaceClient {"
        },
        {
          "path": "crates/opencompany-core/src/economy/siwx.rs",
          "line": 189,
          "text": "impl Default for NonceCache {"
        },
        {
          "path": "crates/opencompany-core/src/feedback/board.rs",
          "line": 148,
          "text": "impl Default for BoardQuery {"
        },
        {
          "path": "crates/opencompany-core/src/feedback/github.rs",
          "line": 114,
          "text": "impl Default for RateLimiter {"
        },
        {
          "path": "crates/opencompany-core/src/feedback/service.rs",
          "line": 57,
          "text": "impl Default for FeedbackFiler {"
        },
        {
          "path": "crates/opencompany-core/src/feedback/tinyhumans.rs",
          "line": 144,
          "text": "impl Default for MockTinyHumansClient {"
        },
        {
          "path": "crates/opencompany-core/src/feedback/triage.rs",
          "line": 411,
          "text": "impl Default for QualityLedger {"
        },
        {
          "path": "crates/opencompany-core/src/harness/built_in/mod.rs",
          "line": 2752,
          "text": "impl Default for HarnessPool {"
        },
        {
          "path": "crates/opencompany-core/src/harness/built_in/policy.rs",
          "line": 459,
          "text": "impl Default for ApprovalRequestQueue {"
        },
        {
          "path": "crates/opencompany-core/src/harness/built_in/provider.rs",
          "line": 1312,
          "text": "impl Default for MockProvider {"
        },
        {
          "path": "crates/opencompany-core/src/harness/pages_tools.rs",
          "line": 145,
          "text": "impl Default for PageManifest {"
        },
        {
          "path": "crates/opencompany-core/src/ports/brain.rs",
          "line": 95,
          "text": "impl Default for Cognition {"
        },
        {
          "path": "crates/opencompany-core/src/ports/types.rs",
          "line": 3312,
          "text": "impl Default for TurnStep {"
        },
        {
          "path": "crates/opencompany-core/src/ports/workflow_runner.rs",
          "line": 920,
          "text": "impl Default for RunCancel {"
        },
        {
          "path": "crates/opencompany-core/src/runtime/delegation.rs",
          "line": 106,
          "text": "impl Default for ChatTarget<'_> {"
        },
        {
          "path": "crates/opencompany-core/src/runtime/run_supervisor.rs",
          "line": 97,
          "text": "impl Default for RunSupervisor {"
        },
        {
          "path": "crates/opencompany-core/src/runtime/workspace_quota.rs",
          "line": 155,
          "text": "impl Default for WorkspaceQuota {"
        }
      ],
      "truncated": true
    },
    "search \"reasoning_effort\"": {
      "outcome": "hits",
      "hits": [
        {
          "path": "crates/opencompany-app/tests/acp_live_smoke.rs",
          "line": 370,
          "text": "                    \"id\": \"reasoning_effort\","
        },
        {
          "path": "crates/opencompany-core/src/company/inference/dialect.rs",
          "line": 662,
          "text": "                \"400: Extra inputs are not permitted: 'reasoning_effort'\","
        },
        {
          "path": "docs/plans/hivemind-handoff.md",
          "line": 130,
          "text": "2. Consider `reasoning_effort` on the ladder's `agentic-v1` alias if"
        },
        {
          "path": "vendor/tinyhivemind/docs/research/grok-bots/opengrok.md",
          "line": 233,
          "text": "  `thinking`/`reasoning_effort`/`chat_template_kwargs` fields synthesized."
        }
      ],
      "truncated": false
    },
    "search \"struct GatewayModel\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"struct Model\"": {
      "outcome": "hits",
      "hits": [
        {
          "path": "crates/opencompany-core/src/company/inference/store.rs",
          "line": 900,
          "text": "pub struct ModelChoice {"
        },
        {
          "path": "crates/opencompany-core/src/metering/model.rs",
          "line": 93,
          "text": "pub struct ModelSlug(&'static str);"
        },
        {
          "path": "crates/opencompany-core/src/server/inference_models.rs",
          "line": 456,
          "text": "pub(crate) struct ModelCatalogCache {"
        },
        {
          "path": "crates/opencompany-core/src/server/ops/inference.rs",
          "line": 103,
          "text": "struct ModelCatalogDto {"
        },
        {
          "path": "docs/key-reworks/phase-2b-default-shape.md",
          "line": 234,
          "text": "pub struct ModelChoice {"
        },
        {
          "path": "docs/key-reworks/phase-5a-routes-carry-part2.md",
          "line": 131,
          "text": "   `git grep -n \"fn load_default\\b\\|fn set_default_choice\\|pub struct ModelChoice\\|pub enum DefaultChoice\\|fn check_model_id\" src`."
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/agent/cost.rs",
          "line": 36,
          "text": "pub(crate) struct ModelPricing {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/agent/tinyagents/summarize.rs",
          "line": 57,
          "text": "pub(super) struct ModelSummarizer {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/channels/routes.rs",
          "line": 27,
          "text": "struct ModelCacheState {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/channels/routes.rs",
          "line": 32,
          "text": "struct ModelCacheEntry {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/channels/tests/common.rs",
          "line": 294,
          "text": "pub(super) struct ModelCaptureModel {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/config/ops/model.rs",
          "line": 9,
          "text": "pub struct ModelSettingsPatch {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/config/schema/dashboard.rs",
          "line": 68,
          "text": "pub struct ModelHealthConfig {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/config/schema/identity_cost.rs",
          "line": 102,
          "text": "pub struct ModelPricing {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/config/schema/routes.rs",
          "line": 7,
          "text": "pub struct ModelRouteConfig {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/config/schema/types/config.rs",
          "line": 12,
          "text": "pub struct ModelRegistryEntry {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/config/schemas/helpers.rs",
          "line": 11,
          "text": "pub(super) struct ModelRouteUpdate {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/config/schemas/helpers.rs",
          "line": 38,
          "text": "pub(super) struct ModelSettingsUpdate {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/desktop/dashboard/types.rs",
          "line": 14,
          "text": "pub struct ModelHealthEntry {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/desktop/dashboard/types.rs",
          "line": 33,
          "text": "pub struct ModelHealthConfigView {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/desktop/dashboard/types.rs",
          "line": 41,
          "text": "pub struct ModelHealthResponse {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/inference/embeddings/rpc/served_models.rs",
          "line": 14,
          "text": "    struct ModelEntry {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/inference/embeddings/rpc/served_models.rs",
          "line": 18,
          "text": "    struct ModelsResponse {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/inference/http/types.rs",
          "line": 91,
          "text": "pub struct ModelsResponse {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/inference/http/types.rs",
          "line": 97,
          "text": "pub struct ModelObject {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/inference/local/service/model_rpc.rs",
          "line": 16,
          "text": "pub(super) struct ModelRpcOutcome {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/inference/presets.rs",
          "line": 92,
          "text": "pub struct ModelPreset {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/inference/provider/ops/models/types.rs",
          "line": 6,
          "text": "pub struct ModelInfo {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/inference/provider/ops_tests.rs",
          "line": 20,
          "text": "struct ModelProbeState {"
        },
        {
          "path": "vendor/openhuman/crates/openhuman-core/src/platform/cost/catalog.rs",
          "line": 51,
          "text": "pub struct ModelPrice {"
        }
      ],
      "truncated": true
    },
    "search \"struct ModelConfig\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"struct Price\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"trait GatewayModel\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"trait Price\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"type GatewayModel\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    },
    "search \"type Price\"": {
      "outcome": "hits",
      "hits": [],
      "truncated": false
    }
  }
}
