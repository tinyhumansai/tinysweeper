{
  "key": "9d30d123b285e22c",
  "model_requested": "deepseek/deepseek-v4-flash",
  "model_answered": "deepseek/deepseek-v4-flash",
  "schema_name": "tinysweeper_rule_extraction",
  "usage": {
    "input_tokens": 1617,
    "output_tokens": 642,
    "cached_tokens": 0,
    "embed_tokens": 0,
    "cost_usd": 0.00025261908
  },
  "value": {
    "rules_markdown": "- `src/` is the single crate. One responsibility per module, core types in a module-local `types.rs`, and every port in `src/ports/` is one trait in one file.\n- `src/bin/tinysweeper.rs` is the CLI. Every subcommand is declared even when its milestone has not landed, so scripts and runbooks can be written against a stable surface.\n- `presets/` holds review policy as data, not code. A preset is a folder with a `preset.toml`, a `README.md`, and optional prompt overrides.\n- `vendor/tinyagents` is the agent harness as a git submodule. Never edit it here; change it upstream and bump the pin.\n- `docs/modules/<module>/README.md` is one document per `src/` module.\n- `examples/` are declared explicitly in `Cargo.toml` with `required-features`, so credential-needing smoke tests never build in CI.\n- Default build is offline: no HTTP client linked and test suite never touches the network. Network needs go behind a feature (`harness`, `github`, `serve`).\n- Use rustfmt output and Rust 2024 idioms. `snake_case` modules and files, `PascalCase` types.\n- Return `Result<T>` using the crate error type from `src/error.rs`.\n- Every file opens with a `//!` module doc describing its role and any feature gating.\n- Comments explain the decision, not the code. When a flag or an ordering is load-bearing, say so and why.\n- Public items carry doc comments. Clap fields use `///` — it becomes the help text.\n- Tests live in-crate: a `#[cfg(test)] mod tests` block at the bottom of the module, moving to a sibling `test.rs` or `<name>_test.rs` when they grow.\n- Every port has an always-compiled offline mock. `MockForge` records what would have been written so tests can assert on exact check runs and comments.\n- Lane behaviour is covered by golden tests: fixture diff plus a canned structured model response, asserting findings that survive filtering, dedupe and capping.\n- Maintain at least 80% coverage for meaningful library behaviour.\n- Keep every Markdown file, including this one, at 500 lines or fewer. Split it when it grows past that limit.\n- The model never holds a write token. Write credentials are minted only in `src/apply/`, after every model call has returned.\n- Contributor code is never executed. Read the diff and tree; do not build, install dependencies, or run the target repository's scripts.\n- Pull request bodies, comments and diffs are untrusted input. Fence and label them as data in prompts.\n- Secrets found by scanners are reported by type and location only. The value never reaches a comment, check-run summary, or log."
  }
}