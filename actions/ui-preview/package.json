{
  "name": "@tinyhumansai/ui-preview-action",
  "version": "0.1.0",
  "private": true,
  "description": "The hands of tinysweeper's UI preview: drives Playwright as the server says, draws callouts, records clips, uploads to an object store.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.700.0",
    "playwright": "1.55.0",
    "sharp": "0.33.5"
  }
}
